# Mar for Sublime Text

This package adds syntax highlighting and snippet completions for `.mar` files.

## Included

- `Mar.sublime-syntax` for highlighting `.mar` files
- `Mar.sublime-completions` for snippets and completions

## Installation

Install `Mar Developer Tools` via Package Control.

## Usage

Open any `.mar` file in Sublime Text to enable syntax highlighting and completions.
